<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เปลี่ยนสีพื้นหลัง - Color Changer</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 20px;
            transition: background 0.8s ease;
        }

        .container {
            background: rgba(255, 255, 255, 0.95);
            padding: 50px;
            border-radius: 25px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.2);
            text-align: center;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .title {
            font-size: 3rem;
            color: #333;
            margin-bottom: 15px;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }

        .subtitle {
            color: #666;
            margin-bottom: 40px;
            font-size: 1.2rem;
        }

        .button-container {
            display: flex;
            gap: 25px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .color-btn {
            padding: 18px 35px;
            border: none;
            border-radius: 15px;
            font-size: 1.3rem;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            color: white;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            position: relative;
            overflow: hidden;
        }

        .color-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: left 0.5s ease;
        }

        .color-btn:hover::before {
            left: 100%;
        }

        .red-btn {
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%);
        }

        .red-btn:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 25px rgba(255, 107, 107, 0.4);
        }

        .green-btn {
            background: linear-gradient(135deg, #51cf66 0%, #40c057 100%);
        }

        .green-btn:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 25px rgba(81, 207, 102, 0.4);
        }

        .blue-btn {
            background: linear-gradient(135deg, #339af0 0%, #228be6 100%);
        }

        .blue-btn:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 8px 25px rgba(51, 154, 240, 0.4);
        }

        .color-btn:active {
            transform: translateY(-1px) scale(1.02);
        }

        .color-btn.active {
            transform: translateY(-5px) scale(1.1);
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .status-text {
            margin-top: 30px;
            font-size: 1.1rem;
            color: #555;
            padding: 15px;
            background: rgba(255, 255, 255, 0.7);
            border-radius: 10px;
            border: 2px dashed #ccc;
        }

        @media (max-width: 600px) {
            .container {
                padding: 30px 20px;
            }
            
            .title {
                font-size: 2.2rem;
            }
            
            .button-container {
                flex-direction: column;
                align-items: center;
            }
            
            .color-btn {
                width: 200px;
            }
        }

        /* Background color classes */
        .bg-red {
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a52 100%) !important;
        }

        .bg-green {
            background: linear-gradient(135deg, #51cf66 0%, #40c057 100%) !important;
        }

        .bg-blue {
            background: linear-gradient(135deg, #339af0 0%, #228be6 100%) !important;
        }

        .bg-default {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="title">🎨 เปลี่ยนสีพื้นหลัง</h1>
        <p class="subtitle">คลิกปุ่มเพื่อเปลี่ยนสีพื้นหลังของหน้าเว็บ</p>
        
        <div class="button-container">
            <button id="redBtn" class="color-btn red-btn">
                🔴 แดง
            </button>
            
            <button id="greenBtn" class="color-btn green-btn">
                🟢 เขียว
            </button>
            
            <button id="blueBtn" class="color-btn blue-btn">
                🔵 น้ำเงิน
            </button>
        </div>
        
        <div id="statusText" class="status-text">
            🌈 เลือกสีที่คุณชอบ!
        </div>
    </div>

    <script>
        // รับ elements ที่ต้องใช้
        const body = document.body;
        const redBtn = document.getElementById('redBtn');
        const greenBtn = document.getElementById('greenBtn');
        const blueBtn = document.getElementById('blueBtn');
        const statusText = document.getElementById('statusText');
        const allButtons = [redBtn, greenBtn, blueBtn];

        // ฟังก์ชันสำหรับลบ active class จากปุ่มทั้งหมด
        function removeActiveFromAll() {
            allButtons.forEach(btn => btn.classList.remove('active'));
        }

        // ฟังก์ชันสำหรับอัพเดทสถานะ
        function updateStatus(color, colorName) {
            statusText.innerHTML = `✨ พื้นหลังเปลี่ยนเป็นสี${colorName}แล้ว!`;
            statusText.style.color = color === 'default' ? '#555' : '#333';
        }

        // Event Listener สำหรับปุ่มแดง
        redBtn.addEventListener('click', function() {
            body.className = 'bg-red';
            removeActiveFromAll();
            redBtn.classList.add('active');
            updateStatus('#ee5a52', 'แดง');
            
            // เพิ่มเอฟเฟกต์พิเศษ
            setTimeout(() => {
                redBtn.classList.remove('active');
            }, 1000);
        });

        // Event Listener สำหรับปุ่มเขียว
        greenBtn.addEventListener('click', function() {
            body.className = 'bg-green';
            removeActiveFromAll();
            greenBtn.classList.add('active');
            updateStatus('#40c057', 'เขียว');
            
            // เพิ่มเอฟเฟกต์พิเศษ
            setTimeout(() => {
                greenBtn.classList.remove('active');
            }, 1000);
        });

        // Event Listener สำหรับปุ่มน้ำเงิน
        blueBtn.addEventListener('click', function() {
            body.className = 'bg-blue';
            removeActiveFromAll();
            blueBtn.classList.add('active');
            updateStatus('#228be6', 'น้ำเงิน');
            
            // เพิ่มเอฟเฟกต์พิเศษ
            setTimeout(() => {
                blueBtn.classList.remove('active');
            }, 1000);
        });

        // เพิ่ม Event Listener สำหรับ keyboard shortcuts
        document.addEventListener('keydown', function(event) {
            switch(event.key.toLowerCase()) {
                case 'r':
                    redBtn.click();
                    break;
                case 'g':
                    greenBtn.click();
                    break;
                case 'b':
                    blueBtn.click();
                    break;
                case 'escape':
                    body.className = 'bg-default';
                    removeActiveFromAll();
                    updateStatus('default', 'เริ่มต้น');
                    break;
            }
        });

        // เพิ่มคำแนะนำ keyboard shortcuts
        setTimeout(() => {
            statusText.innerHTML += '<br><small>💡 เคล็ดลับ: กด R, G, B หรือ ESC บนคีย์บอร์ด</small>';
        }, 3000);

        // เพิ่มเสียงเอฟเฟกต์เมื่อคลิก (ถ้าเบราว์เซอร์รองรับ)
        allButtons.forEach(btn => {
            btn.addEventListener('click', function() {
                // สร้างเสียง click แบบง่าย
                const audio = new Audio('data:audio/wav;base64,UklGRnoGAABXQVZFZm10IBAAAAABAAEAQB8AAEAfAAABAAgAZGF0YQoGAACBhYqFbF1fdJivrJBhNjVgodDbq2EcBj+a2/LDciUFLIHO8tiJNwgZaLvt559NEAxQp+PwtmMcBjiR1/LMeSwFJHfH8N2QQAoUXrTp66hVFApGn+DyvmwhBSuBzvLZiTYIG2m98OScTgwOUarm7blmGgU7k9n1unEiBC13yO/eizEIHWq+8+OWT');
            });
        });
    </script>
<script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'97489bb271064f5d',t:'MTc1NjA5ODg5MS4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>
