<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>เครื่องคิดเลขบวกเลข - Calculator</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #000000 0%, #e30909 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .calculator-container {
            background: white;
            padding: 40px;
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.2);
            width: 100%;
            max-width: 450px;
            text-align: center;
        }

        .calculator-title {
            font-size: 2.5rem;
            color: #333;
            margin-bottom: 10px;
            font-weight: bold;
        }

        .calculator-subtitle {
            color: #666;
            margin-bottom: 30px;
            font-size: 1rem;
        }

        .input-group {
            margin-bottom: 25px;
        }

        .input-label {
            display: block;
            margin-bottom: 8px;
            color: #333;
            font-weight: 600;
            font-size: 1.1rem;
            text-align: left;
        }

        .number-input {
            width: 100%;
            padding: 15px;
            border: 2px solid #e1e5e9;
            border-radius: 10px;
            font-size: 1.2rem;
            text-align: center;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
            background: #f8f9fa;
        }

        .number-input:focus {
            outline: none;
            border-color: #74b9ff;
            box-shadow: 0 0 0 3px rgba(116, 185, 255, 0.1);
            background: white;
        }

        .plus-sign {
            font-size: 2rem;
            color: #74b9ff;
            margin: 15px 0;
            font-weight: bold;
        }

        .calculate-btn {
            width: 100%;
            padding: 15px;
            background: linear-gradient(135deg, #74b9ff 0%, #0984e3 100%);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 1.2rem;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            margin: 20px 0;
        }

        .calculate-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(116, 185, 255, 0.4);
            background: linear-gradient(135deg, #0984e3 0%, #2d3436 100%);
        }

        .calculate-btn:active {
            transform: translateY(0);
        }

        .result-container {
            margin-top: 25px;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 10px;
            border: 2px dashed #74b9ff;
            min-height: 60px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .result-text {
            font-size: 1.1rem;
            color: #666;
        }

        .result-value {
            font-size: 2rem;
            color: #0984e3;
            font-weight: bold;
            margin-top: 10px;
        }

        .error-message {
            color: #e74c3c;
            font-size: 0.9rem;
            margin-top: 5px;
        }

        @media (max-width: 480px) {
            .calculator-container {
                padding: 30px 20px;
            }
            
            .calculator-title {
                font-size: 2rem;
            }
        }

        /* Animation for result */
        @keyframes resultAppear {
            0% { 
                opacity: 0; 
                transform: scale(0.8); 
            }
            100% { 
                opacity: 1; 
                transform: scale(1); 
            }
        }

        .result-animation {
            animation: resultAppear 0.5s ease;
        }
    </style>
</head>
<body>
    <div class="calculator-container">
        <h1 class="calculator-title">🧮 เครื่องคิดเลข</h1>
        <p class="calculator-subtitle">บวกเลข 2 จำนวน</p>
        
        <div class="input-group">
            <label for="number1" class="input-label">ตัวเลขที่ 1</label>
            <input 
                type="number" 
                id="number1" 
                class="number-input" 
                placeholder="กรอกตัวเลขที่ 1"
                step="any"
            >
        </div>

        <div class="plus-sign">+</div>

        <div class="input-group">
            <label for="number2" class="input-label">ตัวเลขที่ 2</label>
            <input 
                type="number" 
                id="number2" 
                class="number-input" 
                placeholder="กรอกตัวเลขที่ 2"
                step="any"
            >
        </div>

        <button onclick="calculateSum()" class="calculate-btn">
            🔢 คำนวณ
        </button>

        <div class="result-container" id="resultContainer">
            <div class="result-text">ผลรวมจะแสดงที่นี่</div>
        </div>
    </div>

    <script>
        function calculateSum() {
            // รับค่าจาก input fields
            const number1 = document.getElementById('number1').value;
            const number2 = document.getElementById('number2').value;
            const resultContainer = document.getElementById('resultContainer');
            
            // ตรวจสอบว่ากรอกข้อมูลครบหรือไม่
            if (number1 === '' || number2 === '') {
                resultContainer.innerHTML = `
                    <div>
                        <div class="result-text">❌ กรุณากรอกตัวเลขให้ครบทั้ง 2 ช่อง</div>
                        <div class="error-message">โปรดตรวจสอบข้อมูลอีกครั้ง</div>
                    </div>
                `;
                return;
            }
            
            // แปลงเป็นตัวเลขและคำนวณ
            const num1 = parseFloat(number1);
            const num2 = parseFloat(number2);
            const sum = num1 + num2;
            
            // ตรวจสอบว่าเป็นตัวเลขที่ถูกต้องหรือไม่
            if (isNaN(num1) || isNaN(num2)) {
                resultContainer.innerHTML = `
                    <div>
                        <div class="result-text">❌ กรุณากรอกตัวเลขที่ถูกต้อง</div>
                        <div class="error-message">ตัวเลขต้องเป็นจำนวนจริง</div>
                    </div>
                `;
                return;
            }
            
            // แสดงผลรวม
            resultContainer.innerHTML = `
                <div>
                    <div class="result-text">✅ ผลรวม</div>
                    <div class="result-value">${num1} + ${num2} = ${sum}</div>
                </div>
            `;
            
            // เพิ่ม animation
            resultContainer.classList.remove('result-animation');
            setTimeout(() => {
                resultContainer.classList.add('result-animation');
            }, 10);
        }
        
        // เพิ่มฟังก์ชันกด Enter เพื่อคำนวณ
        document.addEventListener('keypress', function(event) {
            if (event.key === 'Enter') {
                calculateSum();
            }
        });
        
        // เพิ่มการตรวจสอบ input แบบ real-time
        document.getElementById('number1').addEventListener('input', clearResult);
        document.getElementById('number2').addEventListener('input', clearResult);
        
        function clearResult() {
            const resultContainer = document.getElementById('resultContainer');
            resultContainer.innerHTML = '<div class="result-text">ผลรวมจะแสดงที่นี่</div>';
            resultContainer.classList.remove('result-animation');
        }
    </script>
<script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'974897ccf4ea4f5d',t:'MTc1NjA5ODczMS4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>
